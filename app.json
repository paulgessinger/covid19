{
  "scripts": {
    "dokku": {
      "predeploy": "pip install -r requirements.txt && jupyter execute Covid.ipynb && jupyter nbconvert --to=html Covid.ipynb && mkdir output && mv Covid.html output/index.html"
    }
  }
}
